# Sets - sets are like list sets of collection.
# unlike list : ordered allow duplicates,
# sets are unordered, does not allow duplicates,
# do not support indexing.
# mutable : remove, add. / allowed sets to use diff. data
# list [], sets {}

# add     : persons.add("mark")
# remove  : persons.remove("joy")
# checks  : print("joy" in persons)

persons = {"doji", "alex", "joy", "jenny"}

print("kevin" in persons)
